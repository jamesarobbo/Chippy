function update_total(dropdown){
    var quantity = dropdown.value;
	var name = $(dropdown).closest("td").prev().html();
	$.ajax({
		
		url: "/orders/new/basket_update",
		data: {quantity: quantity, name: name},
		dataType: "script"
		

	});


};


